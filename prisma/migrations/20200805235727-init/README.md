# Migration `20200805235727-init`

This migration has been generated by ChauLe19 at 8/5/2020, 11:57:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "User" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"email" TEXT NOT NULL,
"username" TEXT NOT NULL,
"password" TEXT NOT NULL)

CREATE TABLE "Book" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"dateCreated" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP,
"dateForSale" DATE ,
"price" REAL ,
"forSale" BOOLEAN NOT NULL DEFAULT false,
"isbn" REAL NOT NULL,
"ownedById" INTEGER NOT NULL,
FOREIGN KEY ("ownedById") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Transaction" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"date" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP,
"productId" INTEGER NOT NULL,
"price" REAL NOT NULL,
"sellerEmail" TEXT NOT NULL,
"buyerEmail" TEXT NOT NULL,
FOREIGN KEY ("buyerEmail") REFERENCES "User"("email") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("productId") REFERENCES "Book"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("sellerEmail") REFERENCES "User"("email") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email" ON "User"("email")

CREATE UNIQUE INDEX "User.username" ON "User"("username")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200805235727-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,42 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+model User {
+  id                                        Int           @default(autoincrement()) @id
+  email                                     String        @unique
+  username                                  String        @unique
+  password                                  String
+  ownBooks                                      Book[]
+  buyTransactions                         Transaction[] @relation("BuyTransaction")
+  sellTransactions                           Transaction[] @relation("SellTransaction")
+}
+
+model Book {
+  id          Int           @default(autoincrement()) @id
+  dateCreated DateTime      @default(now())
+  dateForSale  DateTime?
+  price       Float?
+  forSale     Boolean       @default(false)
+  isbn        Float
+  ownedById   Int
+  ownedBy        User          @relation(fields: [ownedById], references: [id])
+  transactions Transaction[]
+}
+
+model Transaction {
+  id                                 Int      @default(autoincrement()) @id
+  date                               DateTime @default(now())
+  productId                          Int
+  price                              Float
+  sellerEmail                        String
+  buyerEmail                         String
+  User_Transaction_buyerEmailToUser  User     @relation("BuyTransaction", fields: [buyerEmail], references: [email])
+  Book                               Book     @relation(fields: [productId], references: [id])
+  User_Transaction_sellerEmailToUser User     @relation("SellTransaction", fields: [sellerEmail], references: [email])
+}
```



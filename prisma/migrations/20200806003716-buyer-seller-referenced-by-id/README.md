# Migration `20200806003716-buyer-seller-referenced-by-id`

This migration has been generated by ChauLe19 at 8/6/2020, 12:37:16 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "User" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"email" TEXT NOT NULL,
"username" TEXT NOT NULL,
"password" TEXT NOT NULL)

CREATE TABLE "Book" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"dateCreated" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP,
"dateForSale" DATE ,
"price" REAL ,
"forSale" BOOLEAN NOT NULL DEFAULT false,
"isbn" REAL NOT NULL,
"ownedById" INTEGER NOT NULL,
FOREIGN KEY ("ownedById") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Transaction" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"date" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP,
"productId" INTEGER NOT NULL,
"price" REAL NOT NULL,
"sellerId" INTEGER NOT NULL,
"buyerId" INTEGER NOT NULL,
FOREIGN KEY ("productId") REFERENCES "Book"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("buyerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("sellerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email" ON "User"("email")

CREATE UNIQUE INDEX "User.username" ON "User"("username")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200806000631-fix-something..20200806003716-buyer-seller-referenced-by-id
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model User {
   id                                        Int           @default(autoincrement()) @id
@@ -32,11 +32,11 @@
 model Transaction {
   id                                 Int      @default(autoincrement()) @id
   date                               DateTime @default(now())
   productId                          Int
+  product                               Book     @relation(fields: [productId], references: [id])
   price                              Float
-  sellerEmail                        String
-  buyerEmail                         String
-  buyer  User     @relation("BuyTransaction", fields: [buyerEmail], references: [email])
-  product                               Book     @relation(fields: [productId], references: [id])
-  seller User     @relation("SellTransaction", fields: [sellerEmail], references: [email])
+  sellerId                        Int
+  buyerId                         Int
+  buyer  User     @relation("BuyTransaction", fields: [buyerId], references: [id])
+  seller User     @relation("SellTransaction", fields: [sellerId], references: [id])
 }
```


